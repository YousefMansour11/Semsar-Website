# Semsar Admin Dashboard

Admin dashboard for managing properties, projects, units, leads, bookings, and settings.

## Setup

```bash
npm install
npm run dev     # http://localhost:8080
npm run build   # production build
```
